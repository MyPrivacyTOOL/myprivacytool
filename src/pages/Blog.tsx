import { useParams } from 'react-router-dom';\nimport blogPosts from '@/data/blog-posts.json';\nimport { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';\nimport { Button } from '@/components/ui/button';\nimport { Badge } from '@/components/ui/badge';\nimport { Breadcrumb, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator } from '@/components/ui/breadcrumb';\nimport { useNavigate, Link } from 'react-router-dom';\n\nconst Blog = () => {\n  const { slug } = useParams();\n  const navigate = useNavigate();\n  const post = slug ? blogPosts.find((p) => p.slug === slug) : null;\n\n  if (slug && !post) {\n    return (\n      <div className=\"min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 flex items-center justify-center\">\n        <div className=\"text-center\">\n          <h1 className=\"text-4xl font-bold text-white mb-4\">Post Not Found</h1>\n          <p className=\"text-gray-300 mb-8\">The blog post you're looking for doesn't exist.</p>\n          <Button onClick={() => navigate('/blog')} className=\"bg-purple-600 hover:bg-purple-700\">\n            Back to Blog\n          </Button>\n        </div>\n      </div>\n    );\n  }\n\n  // Detail view\n  if (post) {\n    return (\n      <div className=\"min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900\">\n        <div className=\"container mx-auto px-4 py-12\">\n          {/* Breadcrumb */}\n          <div className=\"mb-8\">\n            <Breadcrumb>\n              <BreadcrumbList>\n                <BreadcrumbItem>\n                  <BreadcrumbLink href=\"/\" className=\"text-gray-300 hover:text-white\">\n                    Home\n                  </BreadcrumbLink>\n                </BreadcrumbItem>\n                <BreadcrumbSeparator className=\"text-gray-600\" />\n                <BreadcrumbItem>\n                  <BreadcrumbLink href=\"/blog\" className=\"text-gray-300 hover:text-white\">\n                    Blog\n                  </BreadcrumbLink>\n                </BreadcrumbItem>\n                <BreadcrumbSeparator className=\"text-gray-600\" />\n                <BreadcrumbItem>\n                  <BreadcrumbPage className=\"text-purple-400\">{post.title}</BreadcrumbPage>\n                </BreadcrumbItem>\n              </BreadcrumbList>\n            </Breadcrumb>\n          </div>\n\n          <div className=\"max-w-3xl mx-auto\">\n            {/* Post Header */}\n            <article className=\"bg-slate-800/50 backdrop-blur border border-purple-500/20 rounded-lg overflow-hidden\">\n              {post.image && (\n                <img src={post.image} alt={post.title} className=\"w-full h-96 object-cover\" />\n              )}\n              <div className=\"p-8\">\n                <div className=\"flex items-center gap-2 mb-4\">\n                  <Badge className=\"bg-purple-600 hover:bg-purple-700\">{post.category}</Badge>\n                  <span className=\"text-sm text-gray-400\">{post.readTime}</span>\n                </div>\n                <h1 className=\"text-4xl font-bold text-white mb-4\">{post.title}</h1>\n                <div className=\"flex items-center justify-between border-t border-gray-700 pt-4 mt-4\">\n                  <div>\n                    <p className=\"text-gray-400 text-sm\">By {post.author}</p>\n                    <p className=\"text-gray-500 text-sm\">{new Date(post.date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}</p>\n                  </div>\n                </div>\n              </div>\n            </article>\n\n            {/* Post Content */}\n            <div className=\"mt-8 prose prose-invert max-w-none\">\n              <div\n                className=\"text-gray-100 leading-relaxed\"\n                dangerouslySetInnerHTML={{\n                  __html: post.content\n                    .replace(/<h2>/g, '<h2 class=\"text-2xl font-bold text-white mt-8 mb-4\">')\n                    .replace(/<h3>/g, '<h3 class=\"text-xl font-bold text-purple-300 mt-6 mb-3\">')\n                    .replace(/<p>/g, '<p class=\"mb-4 text-gray-300\">')\n                    .replace(/<ul>/g, '<ul class=\"list-disc list-inside mb-4 space-y-2 text-gray-300\">')\n                    .replace(/<li>/g, '<li class=\"ml-2\">')\n                    .replace(/<strong>/g, '<strong class=\"font-bold text-white\">')\n                }}\n              />\n            </div>\n\n            {/* CTA */}\n            <div className=\"mt-12 bg-gradient-to-r from-purple-600/20 to-pink-600/20 border border-purple-500/30 rounded-lg p-8 text-center\">\n              <h3 className=\"text-xl font-bold text-white mb-3\">Ready to see your exposure?</h3>\n              <p className=\"text-gray-300 mb-6\">\n                Run a free scan to discover where your data really is—and get a plan to reclaim it.\n              </p>\n              <Link to=\"/scan\">\n                <Button className=\"bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-700 hover:to-pink-700 text-white\">\n                  Check My Exposure →\n                </Button>\n              </Link>\n            </div>\n\n            {/* Related Posts */}\n            <div className=\"mt-16\">\n              <h3 className=\"text-2xl font-bold text-white mb-6\">More from our blog</h3>\n              <div className=\"grid gap-6\">\n                {blogPosts\n                  .filter((p) => p.id !== post.id)\n                  .slice(0, 2)\n                  .map((relatedPost) => (\n                    <Link key={relatedPost.id} to={`/blog/${relatedPost.slug}`}>\n                      <Card className=\"bg-slate-800/50 border-purple-500/20 hover:border-purple-500/40 hover:bg-slate-800/70 transition-all cursor-pointer\">\n                        <CardHeader>\n                          <div className=\"flex items-center justify-between\">\n                            <Badge className=\"bg-purple-600/50\">{relatedPost.category}</Badge>\n                            <span className=\"text-xs text-gray-500\">{relatedPost.readTime}</span>\n                          </div>\n                          <CardTitle className=\"text-lg text-white mt-2\">{relatedPost.title}</CardTitle>\n                        </CardHeader>\n                        <CardContent>\n                          <CardDescription className=\"text-gray-400\">{relatedPost.excerpt}</CardDescription>\n                          <p className=\"text-xs text-gray-600 mt-3\">\n                            {new Date(relatedPost.date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}\n                          </p>\n                        </CardContent>\n                      </Card>\n                    </Link>\n                  ))}\n              </div>\n            </div>\n          </div>\n        </div>\n      </div>\n    );\n  }\n\n  // Index view\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900\">\n      <div className=\"container mx-auto px-4 py-12\">\n        {/* Header */}\n        <div className=\"mb-12\">\n          <h1 className=\"text-4xl font-bold text-white mb-4\">Privacy Insights & Resources</h1>\n          <p className=\"text-gray-300 max-w-2xl\">\n            Expert guidance on understanding your data exposure, privacy rights, and how to take control of your personal information.\n          </p>\n        </div>\n\n        {/* Featured Post */}\n        {blogPosts.filter((p) => p.featured)[0] && (\n          <div className=\"mb-12\">\n            <h2 className=\"text-2xl font-bold text-white mb-6\">Featured</h2>\n            <Link to={`/blog/${blogPosts.filter((p) => p.featured)[0].slug}`}>\n              <Card className=\"bg-gradient-to-br from-slate-800/50 to-purple-900/30 border-purple-500/30 hover:border-purple-500/50 overflow-hidden transition-all\">\n                <div className=\"grid md:grid-cols-2 gap-6\">\n                  <div className=\"p-8\">\n                    <Badge className=\"bg-purple-600 mb-4\">{blogPosts.filter((p) => p.featured)[0].category}</Badge>\n                    <h3 className=\"text-2xl font-bold text-white mb-3\">{blogPosts.filter((p) => p.featured)[0].title}</h3>\n                    <p className=\"text-gray-300 mb-4\">{blogPosts.filter((p) => p.featured)[0].excerpt}</p>\n                    <div className=\"flex items-center justify-between\">\n                      <div>\n                        <p className=\"text-sm text-gray-400\">{blogPosts.filter((p) => p.featured)[0].author}</p>\n                        <p className=\"text-xs text-gray-500\">\n                          {new Date(blogPosts.filter((p) => p.featured)[0].date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}\n                        </p>\n                      </div>\n                      <span className=\"text-gray-400 text-sm\">{blogPosts.filter((p) => p.featured)[0].readTime}</span>\n                    </div>\n                  </div>\n                  {blogPosts.filter((p) => p.featured)[0].image && (\n                    <img\n                      src={blogPosts.filter((p) => p.featured)[0].image}\n                      alt={blogPosts.filter((p) => p.featured)[0].title}\n                      className=\"h-full w-full object-cover md:min-h-64\"\n                    />\n                  )}\n                </div>\n              </Card>\n            </Link>\n          </div>\n        )}\n\n        {/* All Posts */}\n        <div>\n          <h2 className=\"text-2xl font-bold text-white mb-6\">Latest Articles</h2>\n          <div className=\"grid gap-6 md:grid-cols-2\">\n            {blogPosts\n              .filter((p) => !p.featured)\n              .map((post) => (\n                <Link key={post.id} to={`/blog/${post.slug}`}>\n                  <Card className=\"h-full bg-slate-800/50 border-purple-500/20 hover:border-purple-500/40 hover:bg-slate-800/70 transition-all cursor-pointer\">\n                    {post.image && <img src={post.image} alt={post.title} className=\"w-full h-48 object-cover\" />}\n                    <CardHeader>\n                      <div className=\"flex items-center justify-between\">\n                        <Badge className=\"bg-purple-600/50\">{post.category}</Badge>\n                        <span className=\"text-xs text-gray-500\">{post.readTime}</span>\n                      </div>\n                      <CardTitle className=\"text-lg text-white mt-2\">{post.title}</CardTitle>\n                    </CardHeader>\n                    <CardContent>\n                      <CardDescription className=\"text-gray-400 mb-4\">{post.excerpt}</CardDescription>\n                      <p className=\"text-xs text-gray-600\">\n                        {new Date(post.date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}\n                      </p>\n                    </CardContent>\n                  </Card>\n                </Link>\n              ))}\n          </div>\n        </div>\n      </div>\n    </div>\n  );\n};\n\nexport default Blog;\n